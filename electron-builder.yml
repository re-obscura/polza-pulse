# Конфигурация electron-builder для Polza Pulse.
# NSIS installer (.exe) для Windows + автообновление через generic provider.

appId: ai.polza.monitor
productName: Polza Pulse
copyright: Copyright © Polza

directories:
  output: release
  buildResources: build

# Файлы, попадающие в сборку (результат esbuild).
files:
  - dist-main/**
  - dist-preload/**
  - dist-renderer/**
  - icons/**
  - package.json

# Иконка приложения (минимум 256x256 .ico или .png) — для установщика и окна.
win:
  icon: icons/icon-256.png
  target:
    - target: nsis
      arch: [x64]

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Polza Pulse

# Автообновление: IIS-виртуальный каталог с NTLM (Windows-аутентификация).
# Файлы latest.yml, .exe и .blockmap раздаются через IIS на Azure DevOps Server:
#   https://s-tfs.intellectika.ru/polza-updates/
# Публикация — вручную или через azure-pipelines.yml.
# electron-builder publish здесь не используется — артефакты копируются отдельно.
